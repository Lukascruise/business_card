name: CI

on:
  pull_request:
    branches: [ develop, main ]
  push:
    branches: [ main ]

jobs:
  backend-ci:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_DB: business_card_db
          POSTGRES_USER: user
          POSTGRES_PASSWORD: password
        ports:
          - 5432:5432
        options: >-
          --health-cmd="pg_isready -U user"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5

    env:
      DATABASE_URL: postgres://user:password@localhost:5432/business_card_db
      SECRET_KEY: ci-secret-key-for-testing-only
      DEBUG: "True"
      ALLOWED_HOSTS: localhost,127.0.0.1
      POSTGRES_HOST: localhost
      POSTGRES_PORT: "5432"
      POSTGRES_DB: business_card_db
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      R2_ACCOUNT_ID: ci
      R2_ACCESS_KEY_ID: ci
      R2_SECRET_ACCESS_KEY: ci
      R2_BUCKET_NAME: ci
      CDN_BASE_URL: https://example.com
      DJANGO_SETTINGS_MODULE: config.settings

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Install uv
        run: |
          curl -LsSf https://astral.sh/uv/install.sh | sh
          echo "$HOME/.cargo/bin" >> $GITHUB_PATH

      - name: Install dependencies
        run: |
          uv sync --frozen

      - name: Ruff (lint)
        run: |
          uv run ruff check .

      - name: Mypy (type check)
        run: |
          uv run mypy .

      - name: Django system check
        run: |
          uv run python manage.py check

      - name: Django migration consistency check
        run: |
          uv run python manage.py makemigrations --check --dry-run

      - name: Django migrate
        run: |
          uv run python manage.py migrate --noinput

      - name: Docker build
        run: |
          docker build -t business-card-backend .
